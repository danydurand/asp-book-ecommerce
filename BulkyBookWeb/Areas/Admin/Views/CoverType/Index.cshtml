@model IEnumerable<CoverType> 
@{
    ViewData["Title"] = "Categories";
}

<div class="container p-3">
    <div class="row p-2">
        <div class="col-6">
            <h3 class="text-primary">Cover Type List</h3>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i>&nbsp;New Cover Type
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped table-responsive" style="width:100%">
        <thead>
            <th>Id</th>
            <th>Name</th>
            <th>Created At</th>
            <th class="text-center">Actions</th>
        </thead>
        <tbody>
            @foreach(var obj in Model)
            {
                <tr>
                    <td>@obj.Id</td>
                    <td>@obj.Name</td>
                    <td>@obj.CreatedAt</td>
                    <td class="text-center">
                        <a asp-controller="CoverType" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-sm btn-warning">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

